.vscode/**
node_modules/**
*.vsix
.gitignore
.git/**
scripts/**

# Per-platform binaries:
# - CI must keep ONLY the matching binary before running vsce package.
# - .gitignore ignores all of them, .vscodeignore allows them but only the
#   one matching this build will be present (others are deleted by
#   scripts/build-platform.js).
# Placeholder files (always kept):
!resources/bin/.gitkeep
!resources/bin/README.md
